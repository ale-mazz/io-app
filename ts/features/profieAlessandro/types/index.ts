/**
 * User profile data
 *
 * @name the name of the user
 * @fiscalCode fiscal code of the user
 * @email email of the user
 */
type ProfileAlessandro = {
  name: string;
  fiscalCode: string;
  email: string;
};

export default ProfileAlessandro;
